# Build process
# check if emcc is available
# check if dist is available, otherwise create it
# check if dist/js is available, otherwise create it
# check if dist/wasm is available, otherwise create it

# Watch process
# Use something like node-watch that checks for file changes to recompile
# and reload the developers web page on a change
